<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<packaging>jar</packaging>
	<groupId>exehda</groupId>
	<artifactId>exehda-module-worb-michel</artifactId>
	<version>1.0-SNAPSHOT</version>
	<name>Exehda worb module project</name>

	<properties>
		<resourceEncoding>UTF-8</resourceEncoding>
		<sourceEncoding>UTF-8</sourceEncoding>
		<javaSourceVersion>1.5</javaSourceVersion>
		<javaTargetVersion>1.5</javaTargetVersion>

		<exehda.dist.dir>${env.EXEHDA_HOME}</exehda.dist.dir>
		<project.dist.dir>${exehda.dist.dir}/lib</project.dist.dir>
	</properties>

	<dependencies>
		<dependency>
			<groupId>exehda</groupId>
			<artifactId>exehda-core</artifactId>
			<version>0.0.2006-02-09</version>
		</dependency>
		<dependency>
			<groupId>isam</groupId>
			<artifactId>isam-util-michel</artifactId>
			<version>1.0-SNAPSHOT</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<encoding>${sourceEncoding}</encoding>
					<debug>true</debug>
					<source>${javaSourceVersion}</source>
					<target>${javaTargetVersion}</target>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<configuration>
					<encoding>${resourceEncoding}</encoding>
				</configuration>
			</plugin>

			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>Prepara projeto para ser executado pelo exehda</id>
						<phase>pre-integration-test</phase>
						<configuration>
							<tasks>
								<copy file="${project.build.directory}/${project.build.finalName}.jar"
									tofile="${project.dist.dir}/testing-worb.jar" verbose="true"/>
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
